# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Build artifacts (none for MVP, but reserve)
dist/
*.tgz
*.zip

# Node (in case we add tooling later)
node_modules/
package-lock.json

# Local development
*.log

# Generated, install-specific monster compendium-art mapping (references the
# user's own locally-installed art module; regenerated by the Monster Art tool).
data/monster-art-mapping.json

# Claude Code per-project state (session/cache — never commit)
.claude/

# Playwright MCP scratch output
.playwright-mcp/

# Fantasy RPG Dings font — licensed, no redistribution (CREDITS.md)
assets/fonts/fantasy-rpg-dings.otf
